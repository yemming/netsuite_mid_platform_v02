{
  "build": {
    "command": "npm run build",
    "installCommand": "npm install"
  },
  "deploy": {
    "startCommand": "cd .next/standalone && PORT=8080 node server.js",
    "healthcheck": {
      "path": "/api/health",
      "interval": 30,
      "timeout": 10
    }
  }
}

