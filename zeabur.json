{
  "build": {
    "command": "npm run build",
    "installCommand": "npm install"
  },
  "deploy": {
    "startCommand": "cd .next/standalone && node server.js",
    "healthcheck": {
      "path": "/api/health",
      "interval": 30,
      "timeout": 10
    }
  }
}

